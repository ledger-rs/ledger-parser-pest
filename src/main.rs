// use ledger_pest_parser::{LedgerParser, Rule};
// use pest::Parser;

fn main() {
    // env_logger::init();
    // log::debug!("starting...");
    println!("Hello World. The logic is in the tests.");
}
